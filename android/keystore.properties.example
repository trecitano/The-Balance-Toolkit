# Copy this file to android/keystore.properties and fill in real values.
# If this file is missing, release builds fall back to debug signing.
# storeFile can be an absolute path or relative to the android/ directory.
storeFile=release-keystore.jks
storePassword=change-me
keyAlias=release
keyPassword=change-me
